/target
**/*.rs.bk
Cargo.lock.orig

# This workspace ships a binary, so the lockfile IS committed
# (overrides a global ignore of Cargo.lock).
!/Cargo.lock
.direnv/
result
result-*

# superpowers ephemeral planning notes
/docs/superpowers/
